# 🔧 Исправление проблемы: данные не загружаются на Vercel

## Проблема
На Vercel не загружаются клиенты и товары из Supabase.

## Причина
Переменные окружения не настроены или проект не пересобран после их добавления.

## ✅ Решение:

### ШАГ 1: Проверь переменные окружения в Vercel

1. Зайди на https://vercel.com
2. Выбери свой проект **shop-sport**
3. Перейди в **Settings** → **Environment Variables**
4. Проверь, что есть ДВЕ переменные:

   **Переменная 1:**
   - Name: `VITE_SUPABASE_URL`
   - Value: `https://lehihjdhzndarctkqkjq.supabase.co`
   - Environment: ✅ Production, ✅ Preview, ✅ Development

   **Переменная 2:**
   - Name: `VITE_SUPABASE_ANON_KEY`
   - Value: `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImxlaGloamRoem5kYXJjdGtxa2pxIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjU2NDU1NjAsImV4cCI6MjA4MTIyMTU2MH0.PHhoU-1RTo3_TAUKjSem9B3hK13EaDAy2a67uPc1yyg`
   - Environment: ✅ Production, ✅ Preview, ✅ Development

### ШАГ 2: Если переменных нет - добавь их

1. Нажми **"Add New"**
2. Добавь первую переменную:
   - Key: `VITE_SUPABASE_URL`
   - Value: `https://lehihjdhzndarctkqkjq.supabase.co`
   - Environment: выбери все три (Production, Preview, Development)
   - Нажми **"Save"**
3. Добавь вторую переменную:
   - Key: `VITE_SUPABASE_ANON_KEY`
   - Value: `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImxlaGloamRoem5kYXJjdGtxa2pxIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjU2NDU1NjAsImV4cCI6MjA4MTIyMTU2MH0.PHhoU-1RTo3_TAUKjSem9B3hK13EaDAy2a67uPc1yyg`
   - Environment: выбери все три (Production, Preview, Development)
   - Нажми **"Save"**

### ШАГ 3: Пересобери проект

После добавления переменных нужно пересобрать проект:

1. Перейди на вкладку **"Deployments"**
2. Найди последний деплой
3. Нажми на три точки (⋮) справа
4. Выбери **"Redeploy"**
5. Или просто сделай новый коммит и push на GitHub - Vercel автоматически пересоберет

### ШАГ 4: Проверь консоль браузера

1. Открой приложение на Vercel
2. Нажми F12 (открыть DevTools)
3. Перейди на вкладку **Console**
4. Проверь, есть ли ошибки или предупреждения о Supabase

Должны быть сообщения:
- `🔗 Подключение к Supabase:` с информацией о URL и ключе
- Если видишь `⚠️ Переменная окружения не найдена` - значит переменные не подхватились

### ШАГ 5: Проверь права доступа в Supabase

1. Зайди на https://supabase.com/dashboard
2. Выбери проект `lehihjdhzndarctkqkjq`
3. Перейди в **Settings** → **API**
4. Убедись, что **anon public key** совпадает с тем, что в переменных окружения

## ✅ После исправления:

- Клиенты должны загружаться в форме создания заказа
- Товары должны загружаться в форме создания заказа
- Можно добавлять новых клиентов
- Можно добавлять новые товары
- Можно создавать заказы

## 🔍 Дополнительная диагностика:

Если проблема осталась, проверь:

1. **RLS (Row Level Security) в Supabase:**
   - Убедись, что таблицы `customers`, `products`, `orders` имеют правильные политики доступа
   - Для теста можно временно отключить RLS и проверить, работает ли

2. **CORS настройки:**
   - В Supabase Dashboard → Settings → API проверь, что домен Vercel добавлен в разрешенные

3. **Логи Vercel:**
   - В Vercel Dashboard → Deployments → выбери деплой → **"Build Logs"** и **"Function Logs"**
   - Проверь, нет ли ошибок при сборке или выполнении

